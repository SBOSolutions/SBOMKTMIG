<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- product.template inherit list view -->
    <record id="season_product_template_view_list_inherit" model="ir.ui.view">
        <field name="name">product.template.inherit.view.tree</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="season_id"/>
            </xpath>
        </field>
    </record>

    <!-- product.template inherit form view -->
    <record id="season_product_template_view_form_inherit" model="ir.ui.view">
        <field name="name">product.template.inherit.view.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='barcode']" position="after">
                <field name="season_id"/>
            </xpath>
        </field>
    </record>

    <record id="product_template_search_view" model="ir.ui.view">
        <field name="name">product.template.search</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="season_id" />
            </xpath>
            <xpath expr="//search[1]/group[1]/filter[@name='categ_id']" position="after">
                <filter name="group_by_season_id" string="Saison" context="{'group_by': 'season_id'}"/>
            </xpath> 
        </field>
    </record>


    <record id="stock.stock_product_normal_action" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="context" eval="{'search_default_season_id': 1}"/>
        <field name="search_view_id" ref="stock.stock_product_search_form_view"/>
    </record>

</odoo>